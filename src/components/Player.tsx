'use client';

// Player disabled for public API version as we don't have full playback.
// Future improvement: Implement HTML5 audio for previewUrl if available on tracks.

export default function Player() {
    return null;
}
